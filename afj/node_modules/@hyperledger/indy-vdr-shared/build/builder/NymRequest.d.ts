import type { WriteRequestResultTxnBase, WriteRequestResponse } from '../types';
import { IndyVdrRequest } from '../indyVdr';
export type NymRequestOptions = {
    submitterDid: string;
    dest: string;
    verkey?: string;
    alias?: string;
    role?: 'STEWARD' | 'TRUSTEE' | 'ENDORSER' | 'NETWORK_MONITOR';
    diddocContent?: string;
    version?: number;
};
interface NymResultTxn extends WriteRequestResultTxnBase {
    type: '1';
    data: {
        dest: string;
        verkey: string;
        alias?: string;
        role?: string;
    };
}
export type NymResponse = WriteRequestResponse<NymResultTxn>;
export declare class NymRequest extends IndyVdrRequest<NymResponse> {
    constructor(options: NymRequestOptions);
}
export {};
