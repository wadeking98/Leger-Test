export type AriesAskarErrorObject = {
    code: number;
    extra?: string;
    message: string;
};
export declare class AriesAskarError extends Error {
    readonly code: number;
    readonly extra?: string;
    constructor({ code, message, extra }: AriesAskarErrorObject);
    static customError({ message }: {
        message: string;
    }): AriesAskarError;
}
export declare function handleInvalidNullResponse<T extends null | unknown>(response: T): Exclude<T, null>;
