export declare const byteBufferToBuffer: (buffer: {
    data: Buffer;
    len: number;
}) => Buffer;
export declare const secretBufferToBuffer: (buffer: {
    data: Buffer;
    len: number;
}) => Buffer;
